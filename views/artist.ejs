<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" lang="en" xml:lang="en">

  <head>
    <title>Albums by <%=item.name%></title>
  </head>

  <body>
  <h1><%=item.name%>'s Albums:</h1>

<!-- how do we establish that this should be a list of albums, not of artists?? -->
    <% if (items.length > 0) { %>
        <ul id="Albums">
        <% items.forEach(function(item) { %>
            <li class="SpecificAlbum">
                <a rel="Album" href="/albums/<%=item._id%>"><%=item.name%></a>,
<!-- how to loop through genres? -->

                 <span class="genre"></span>
            </li>
        <% }); %>
        </ul>
    <% } else { %>
        <div>No albums have been added for <%=item.name%>.</div>
    <% } %>

    </ul>
  </body>

</html>