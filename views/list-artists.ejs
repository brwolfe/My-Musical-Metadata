<!DOCTYPE html>
<html>
  <head>
    <title>BrandCramp: Recording Artists</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <a href="/"><h1>Artists</h1></a>
    <% if (items.length > 0) { %>
    <ul>
      <% items.forEach(function(item) { %>
      <li><a href="/artists/<%=item._id%>"><%=item.name%></a></li>
      <% }); %>
    </ul>
    <% } else { %>
    <div>No artists found.</div>
    <% } %>
    <div class="control">
      <h2>Search for artists by name</h2>
      <form method="get" action="/artists/">
        <label>Artist name: <input name="name"></label>
        <button>Search</button>
      </form>
    </div>
  </body>
</html>
