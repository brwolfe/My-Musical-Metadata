<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Album: <%=item.name%></title>
  </head>
  
  <body>
  
  <h1><%=item.artist%> -- <%=item.name%></h1>

<!-- how do we loop through the songs on the album here? -->
<!-- ...thinking they should be related_items, but then songs need to be an item? -->
    <% if (related_items.length > 0) { %>
    <ol id="Songs">
      <% related_items.forEach(function(related_item) { %>
      <li class="Song">
<!-- what if anything do we link to here? do we want a song template? -->
        <a rel="Song" href=""><%=related_item.name%></a>,

<!-- how do we loop through these properties? -->
        <span class="genre"><%=related_item.genre%></span>, <span class="genre"><%=related_item.genre%></span>,
        <span class="key"><%=related_item.key%></span>, <span class=key"><%=related_item.key%></span>,
        <span class="tempo"><%=related_item.tempo%></span>, <span class="tempo"><%=related_item.tempo%></span>.

      </li>
      <% }); %>
    </ol>
    <% } else { %>
    <div>No songs listed for <%=item.name%>.</div>
    <% } %>

</body>
</html>